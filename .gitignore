# ===== 系统文件 =====
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# ===== IDE / 编辑器 =====
.idea/
.vscode/
*.iml

# ===== 日志 =====
*.log

# ===== Go 后端 =====
# 编译产物：*.exe 不限层级，避免在包目录里 go build 出的二进制漏网
*.exe
*.test
*.out
__debug_bin*

# 本地配置（含密钥与旧库迁移路径，勿提交；模板见 .env.example）
# 用通用规则而不是写死 backend-go/.env：根目录 / 前端目录下的 .env 同样含密钥
.env
.env.*
!.env.example

# 运行数据：SQLite 数据库（含个人隐私数据）与上传的图片/视频，勿提交
# 根锚定的 /data/ /uploads/ 是必要的：DATABASE_PATH、UPLOAD_DIR 的默认值是相对进程
# 工作目录的（且 .env 加载失败会被静默忽略），从仓库根目录启动服务就会在根目录生成
# 这两份数据，只有 backend-go/ 下的规则挡不住
/data/
/uploads/
backend-go/data/
backend-go/uploads/
*.db
*.db-wal
*.db-shm

# ===== 前端 =====
node_modules/
dist/
frontend/.vite/

# 本地开发缓存
*.local
